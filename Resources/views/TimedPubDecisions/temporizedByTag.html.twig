{% extends 'PumukitWebTVBundle::layout.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-xs-12">
            <div class="row">
                <div class="col-xs-12">
                    <h1 class="titulo_widget">{{ title }}</h1>
                </div>
            </div>
            {% include 'PumukitWebTVBundle:List:template_list.html.twig' %}
        </div>
    </div>
{% endblock %}
